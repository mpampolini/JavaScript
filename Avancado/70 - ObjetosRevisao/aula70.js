// Objeto literal = {} formado por chave valor
const pessoa = {
  nome: "Mateus",
  sobrenome: "Pampolini",
};

console.log(pessoa.nome);
console.log(pessoa.sobrenome);
const chave = "nome";
console.log(pessoa[chave]);
console.log(pessoa["sobrenome"]);
